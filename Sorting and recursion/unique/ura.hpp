#pragma once
#include <iostream>
#include <algorithm>

template <typename It>
It unique(It begin, It end) {


// if (begin==end) return begin;
// auto temp = begin;
// while (++begin != end)
//   {
//     if ((*temp != *begin))
//       *(++temp)=*begin;
//   }
// return ++temp;
}

